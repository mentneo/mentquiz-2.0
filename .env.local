# ...existing code...

# Add this for deployed environment
NEXTAUTH_URL=https://your-deployed-app-url.com
NEXT_PUBLIC_BASE_URL=https://your-deployed-app-url.com

# To fix Google OAuth issues
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
NEXTAUTH_SECRET=your-nextauth-secret-key

# Make sure Google OAuth callback URLs are properly configured
# in Google Cloud Console to include your production domain
# e.g., https://your-deployed-app-url.com/api/auth/callback/google
# ...existing code...